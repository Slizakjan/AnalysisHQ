<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <title><%= @test[:exam_name] %> | Exam Portal</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col items-center px-4 py-6 text-gray-800">

  <main class="w-full max-w-3xl">

    <!-- Header -->
    <header class="text-center mb-6">
      <h1 class="text-2xl sm:text-3xl font-semibold text-emerald-600 mb-1"><%= @test[:exam_name] %></h1>
      <p class="text-gray-500 text-sm">Otázek: <%= @questions_count %></p>
    </header>

    <!-- Seznam otázek -->
    <section class="flex flex-col gap-4">
      <% if @questions.empty? %>
        <div class="text-center mt-12 bg-white rounded-xl shadow-md p-8">
          <p class="text-gray-500 text-lg">Žádné otázky nebyly nalezeny.</p>
        </div>
      <% else %>
        <% @questions.each_with_index do |q, idx| %>
          <div onclick="window.location='/exam/<%= @test[:test_id] %>/<%= q[:answer_id] %>'"
               class="flex flex-col bg-white rounded-xl shadow-md p-4 cursor-pointer hover:bg-emerald-50 transition">

            <!-- Otázka -->
            <div class="flex justify-between items-center mb-2">
              <h2 class="text-lg font-medium text-gray-900">Otázka <%= idx + 1 %></h2>
              <div class="text-2xl text-gray-400 select-none">›</div>
            </div>
            <p class="text-sm text-gray-700 mb-3 line-clamp-3">
              <%= q[:question]['question'] rescue 'Bez textu' %>
            </p>

            <!-- Odpovědi -->
            <% if q[:answer_json] && !q[:answer_json].empty? %>
              <ul class="list-disc list-inside text-sm text-gray-600">
                <% q[:answer_json].each do |a| %>
                  <li><%= a %></li>
                <% end %>
              </ul>
            <% else %>
              <p class="text-sm text-gray-400 italic">Žádné odpovědi k dispozici</p>
            <% end %>

          </div>
        <% end %>
      <% end %>
    </section>

  </main>

</body>
</html>
